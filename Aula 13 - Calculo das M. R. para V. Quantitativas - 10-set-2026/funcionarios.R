mean(funcionarios$salario)
mean(funcionarios$idade_anos)

median(funcionarios$salario)
median(funcionarios$idade_anos)

var(funcionarios$salario)
var(funcionarios$idade_anos)

sd(funcionarios$salario)
sd(funcionarios$idade_anos)

sd(funcionarios$salario) / mean(funcionarios$salario) * 100
sd(funcionarios$idade_anos) / mean(funcionarios$idade_anos) * 100
